<div class="wrapper">

  <%= render partial: "leagues/dashboard_jumbotron", locals: {league: @league} %>

   <section class="module-small">

     <div class="container">
       <div class="row">
         <div class="col-sm-6">
           <div class="work-details">
             <h1>League Finances</h1>

             <h3>League Pot: $<%= (@league.managers.count + 1)* (@league.commissioner.due.amount) %></h3>

             <% total = 0 %>

             <h3>Total Collected: $  </h3>

             <h3>Buy In: $<%= @league.commissioner.due.amount  %></h3>

             <h1>Manage Payments</h1>
               <% @managers.each do |manager|  %>
                 <h3 class="manager-paid"><%= manager.name %>
                   <% if manager.due.paid %> paid
                   <% else %> <span id="due-amt">$<%= manager.due.amount %></span><button class: "btn btn-g btn-circle btn-sm" id="paid-btn">paid</button>
                   <% end %><br>
               <% end %><h3>
           </div>
       </div>
     </div>
   </div>

   </section>
